# repo-map — dépendances (installer dans le venv : `uv pip install -r requirements.txt`)
mcp==1.28.1

# tree-sitter : moteur + grammaires (wheels précompilés, aucune compilation C sous Windows)
tree-sitter==0.25.2
tree-sitter-python==0.25.0
tree-sitter-javascript==0.25.0
tree-sitter-typescript==0.23.2
